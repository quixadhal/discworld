Edit the Makefile (or GNUmakefile), port.h, and config.h.

Type 'make' to build the driver.

Be sure to read the ChangeLog for information on the address server
(addr_server) before trying to run the driver.
