/* Top list */
 
inherit "std/object";
 
void setup() {
    set_name("list");
    set_short("List of Top Players");
    set_long("We do not have a top players list yet, sorry.\n");
    set_value(8);
    set_weight(1);
    add_alias("top players list");
    add_alias("list of top players");
}
