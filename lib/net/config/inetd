# The inetd file...  Contains all the non useful info for the
# the services we want to use.
# Format is...  name of service type file to call function on.
finger inetd /net/daemon/in_finger
tell inetd /net/daemon/in_tell
