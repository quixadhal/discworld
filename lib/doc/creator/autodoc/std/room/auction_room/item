.DT
item
Disk World creator help
item

.SH Name
.SP 5 5
item -  In this new version, we aren't keeping the objects loaded, because the auto_load code seems flaky outside of  /global/player.
.EP

.SH Definition
.SI 5
class item {
string name;
string * alias;
string * adjectives;
string long;
string read_mess;
string enchant_mess;
}

.EI

.SH Defined in
.SI 5
/std/room/auction_room.c
.EI

.SH Description
.SP 5 5

In this new version, we aren't keeping the objects loaded,
because the auto_load code seems flaky outside of 
/global/player.c
Instead keep a whole bunch of info about it on file.

.EP

