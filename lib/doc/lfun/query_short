string query_short()
 
        Returns the unadulterated short.   This will return the short
descripton that was set with set_short.  the short procedure is quite
often modified by objects in the inheritance path (ie adding in things like
"(weilded)" on weapons).
 
See also
    set_short, pretty_short, short, query_plural, pretty_plural,
    set_main_plural
