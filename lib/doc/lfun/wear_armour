int|string wear_armour(object armour)

Description
	Makes the living object wear the given armour.  It checks various
things like query the nunmber of items of the given type it can wear off the
race object.  returns 0 if he succeded in wearing it, or returns the
string error message. It is defined in npcs and players.

