void remove_alias(string str)
 
        Removes the alias off the object.  Once this has been removed you
cannot reference this object using that name any more.  Is useful if the
name of the object you are dealing with changes around.
 
See also
    set_name, query_name, add_alias, add_plural, add_adjective
